[project]
name = "datatracker"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.14"
dependencies = [
    "click>=8.3.1",
    "colorama>=0.4.6",
    "pytest>=9.0.2",
]

[project.scripts]
dt = "data_tracker.cli:cli"

[tool.uv]
package = true
